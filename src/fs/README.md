dummy-fuse is a "hello world"-level FUSE filesystem.

Usage:
```
$ dummy-fuse MOUNTPOINT
```

The filesystem is read-only and is populated with a single file `dummy-file.txt` with contents `Hello world!\n`.
